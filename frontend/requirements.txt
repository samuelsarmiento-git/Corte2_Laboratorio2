# --- Framework principal ---
Flask==2.3.3
Werkzeug==3.1.3

# --- Extensiones útiles ---
Flask-WTF==1.2.1          # Formularios
Flask-Login==0.6.3        # Manejo de sesiones y login
Flask-Cors==4.0.0         # Permite peticiones desde otros dominios (útil si conectas backend)
Flask-Bootstrap==3.3.7.1  # Integración sencilla con Bootstrap

# --- Generación de PDF (para historias clínicas) ---
WeasyPrint==61.0

# --- Dependencias de WeasyPrint (Linux y Windows) ---
tinycss2==1.3.0
cairosvg==2.7.1
cairocffi==1.7.1
pillow==10.2.0

# --- Seguridad (JWT o similares, por si lo integras con backend) ---
PyJWT==2.9.0

# --- Conexión HTTP (si luego haces peticiones al backend FastAPI) ---
requests==2.32.3

